# GBF Accelerator v1.6.1

本版本专注于修复静态资源预加载（Prefetch）在跨 Host 场景下的引用处理，并完善底层负缓存的淘汰算法与测试覆盖。

## 修复与优化

### 1. 修复跨 Host 静态资源预加载引用丢失 (P0)
- 修正 extract_asset_refs 在提取场景脚本中的绝对 URL 素材时丢失 Host 域名的问题。
- 提取静态资源时完整保留显式声明的 Akamai CDN 域名（如 prd-game-a*.granbluefantasy.akamaized.net），避免将绝对引用的 CDN 素材错误发往当前主站域名发起预取。
- 相对路径继续安全继承当前页面 Host；同时将提取去重从线性查找优化为哈希集合（O(1) 复杂度）。

### 2. 负缓存 LRU 淘汰机制补全
- 修复负缓存（Negative Disk Cache Index）在读取命中时未刷新 LRU 序列的问题。
- 增加 move_to_end 调用，确保 4096 条缺失文件索引严格遵循最近最少使用淘汰，保证高频缺失路径持久驻留内存。

### 3. 测试套件扩充
- 新增针对静态资源跨 Host 提取、相对路径回退、非法外链阻断的端到端自动化测试（Test 36）。
- 全套 36 项自动化回归测试全部通过。
