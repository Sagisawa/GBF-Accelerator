# GBF Accelerator v1.5.0

## 性能优化

### 1. 事件循环全面去阻塞
- 缓存磁盘读写（整文件读取、原子落盘的 fsync、md5 校验）全部移入后台线程执行，慢速磁盘不再阻塞并发请求与游戏 API 转发。
- gzip 重新压缩级别由 9 降至 6，明显降低落盘时的 CPU 开销。

### 2. 304 重验证元数据短路
- 浏览器带 `If-None-Match` 的条件请求仅读取 `.ext` 元数据侧车即可返回 304 Not Modified，不再把资源整份读入内存；整页刷新场景的磁盘 I/O 大幅减少。

### 3. HTTP/2 上游多路复用
- 上游连接池启用 HTTP/2（h2），缓存未命中时并发拉取资源复用单条连接，免除逐条 TCP+TLS 跨海握手，加快新页面首次加载；h2 缺失时自动回退 HTTP/1.1。

### 4. 资源预加载 (Prefetch)
- 返回场景 JS/JSON 时自动解析其中引用的素材路径，本地缺失的资源交由 3 并发后台队列预热落盘（队列上限 600、单文件引用上限 120，自动过滤外站域名与 API 路径），首次进入新副本/活动更流畅。

### 5. 启动 RAM 预热
- 启动时在后台线程把缓存目录中的小体积高频文件（最多 2000 个）预载入内存缓存，消除会话首读的磁盘延迟。

### 6. 内存缓存准入放宽
- 单文件内存准入上限由 5MB 放宽至 16MB（BGM、大型立绘、战斗动画 Sheet 首次访问即进内存），并随 RAM 总预算自适应收缩（预算的 1/4，下限 4MB），避免小预算被单个大文件霸占。

## 新增功能

### 1. 内存缓存上限 GUI 可调
- 「性能与系统资源选项」中新增「内存缓存上限 (MB)」输入框（16–8192），保存后立即按 LRU 收缩生效，并实时显示当前用量。

### 2. 上游延迟测试
- 底栏新增「延迟测试」按钮：经当前上游代理对游戏服务器分别测量 1 次冷连接（建链 + 首个请求）与 3 次热连接 RTT 并分开显示，便于评估与挑选节点。

### 3. 新版本检查
- 启动时自动检测 GitHub Releases 新版本（可关闭），发现新版时界面角标提醒并弹窗展示更新日志；也可手动「检查更新」。

### 4. 浏览器强缓存默认开启
- `enable_browser_cache` 默认开启；仍仅对 `/assets/<时间戳>/` 等版本化资源注入 `immutable`，未版本化资源不注入，避免游戏更新后吃到陈旧缓存。

## 修复

- 修复一键注销根证书时可能无限等待卡死的问题（certutil 增加超时与自动确认）。
- ACGPower 缓存探测增强：支持检测正在运行的 ACGPower 进程、常见盘符与安装路径；选择上级目录时自动向下校正到真实缓存目录（`cache/gbf/https`）并锁定。
- 窗口宽度改为按实际布局自动测量适配，修复高分屏 / DPI 缩放下底栏右侧按钮与 SHA-256 指纹行被截断的问题；底栏 emoji 图标改为位图渲染，修复文字居中错位。

---

## 验证与完整性
- 缓存读写往返、304 元数据一致性、预加载引用提取、RAM 上限钳制与自适应、代理服务启停等自动化冒烟测试全部通过。
