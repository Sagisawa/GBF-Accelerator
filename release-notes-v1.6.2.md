# GBF Accelerator v1.6.2

本版本重点修复在开启内存热点缓存（RAM Cache）时出现的响应头编码不匹配问题，以及历史磁盘损坏缓存自动自愈机制，并包含上游目录层级自动校正补丁。

## 修复与优化

### 1. 修复新副本无限卡“官方更新弹窗”问题 (P0)
- **根因**：在上游通过 gzip 传输静态素材时，httpx 已经自动对 `resp.content` 进行了透明解压。此前 `store_ram_cache` 误将明文数据标记为了 `Content-Encoding: gzip` 存入 RAM 缓存，导致浏览器第二次访问命中内存时抛出 `net::ERR_CONTENT_DECODING_FAILED`，触发游戏前端 409/更新重载死循环。
- **修复**：`store_ram_cache` 严格以数据本身实际魔数（`\x1f\x8b`）判断是否携带 gzip 响应头，彻底杜绝明文虚标 gzip 导致浏览器解码崩溃。

### 2. 磁盘损坏缓存与 HTML 错误页实时自愈 (P0)
- **根因**：复用旧版本或 ACGPower 历史资源包时，某些异常中断留存的几百字节 HTML 错误页（如 502 Bad Gateway / Cloudflare 403）此前读盘时未作校验即作为 200 OK 脚本返回，导致游戏前端语法解析错误，且在错误弹窗被静默屏蔽后卡死在战斗 Ready 界面。
- **修复**：`get_disk_cache` 与启动预热 `warm_ram_cache` 增加完整性校验（同时支持明文与 gzip 块流式检验）。一旦检测到非合法静态资源或 HTML 错误标记，自动删除损坏文件并回退至网络拉取干净文件。

### 3. 缓存目录归一化与负缓存防呆强化
- 自动校正用户直接选择 `\assets` 子文件夹时的目录层级，锁定到真实缓存根目录。
- 变更缓存目录或探测 ACGP 时自动清空负缓存与重启代理服务，防止出现假缺失（False Miss）。
