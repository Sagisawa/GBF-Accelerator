=================================================================
   GBF 加速器（GBF Accelerator）使用说明
=================================================================

【只需 2 步】：

第 1 步：配置浏览器分流（推荐使用 ZeroOmega 或 SwitchyOmega 插件）
-----------------------------------------------------------------
1. 打开 Chrome / Edge / 任意 Chromium 浏览器，安装 ZeroOmega 扩展。
2. 打开插件设置 -> 【导入/导出】 -> 点击【从备份文件恢复】。
3. 选择本程序同级目录下的【SwitchyOmega_GBF.bak】文件导入。
4. 在浏览器右上角插件图标处，切换选定为【GBF_AutoSwitch】。

（备用方式 1：如果不想装插件，可在软件界面直接勾选【自动配置系统 PAC 代理】；
 备用方式 2：若使用 Clash 开启了 TUN 虚拟网卡模式导致系统 PAC 路由竞争，推荐使用 SmartProxy 插件：添加 127.0.0.1:8124 代理服务器，并在 Bypass 列表中填入 `<local>` 与 `*.granbluefantasy.jp` 等规则分流）

第 2 步：启动本加速器与上游代理
-----------------------------------------------------------------
1. 确保你的 Clash Verge / Clash / v2rayN / Surge 等上游代理已开启并连接到可用节点。
2. 启动本程序（Windows 端双击 GBF_Accelerator.exe，macOS 端打开 GBF_Accelerator.app）。
3. 浏览器打开 game.granbluefantasy.jp 即可开始游戏。
   （移动端建议使用手机 Safari / Chrome 等浏览器访问 game.granbluefantasy.jp 游玩。SkyLeap 内置使用的是 gbf.game.mbga.jp，主要用于登录跳转，不包含游戏静态素材，无法走本地缓存）。

上游代理也支持岛风 GO，默认地址为 `http://127.0.0.1:8099`。修改地址后点击“确认”，会立即断开旧连接并切换到新代理；“自动探测”也会检测该端口。若同时检测到多个代理，会弹出列表供选择。

如果不想经过上游代理，可在界面的“上游网络代理”下勾选“直连模式”。该模式立即切换到本机网络，同时继续使用本地缓存；取消勾选即可立即恢复上游代理。

如需随系统开机自动启动，可在界面中勾选“开机自启”；启动后程序会自动在后台运行（Windows 缩小到系统托盘，macOS 作为后台服务常驻并可随时通过 Web 控制台管理）。该选项默认关闭。

=================================================================
【macOS 用户特别注意事项】：
1. 首次打开提示“无法验证开发者”或“已损坏”：
   - 这是 macOS Gatekeeper 安全机制对未签名开源软件的正常拦截；
   - 解决方法：在【访达】应用程序中找到本程序，按住 Control 键或右键点击，选择【打开】；
   - 或打开系统【终端】（Terminal）执行以下命令解除系统隔离：
     xattr -cr /Applications/GBF_Accelerator.app
2. 根证书安装与信任授权：
   - 点击界面上的【一键安装根证书】时，系统若弹出授权提示，请输入 Mac 锁屏密码或按触控 ID 允许信任。
3. 控制台管理与后台运行：
   - macOS 下程序为纯 Go 原生架构，双击 GBF_Accelerator.app 启动后自动拉起内置 Web 控制台；
   - 关闭浏览器窗口不会中断代理服务，随时在浏览器访问 http://127.0.0.1:8125 即可重新打开管理界面；如需完全退出，在控制台界面点击【停止加速并退出】即可。
=================================================================
【实际效果与限制】：
- 本工具只缓存静态资源（立绘、音频、脚本、图片等）。这类资源首次加载仍需经过网络下载，加速效果体现在之后的重复加载：第二次起由本地磁盘或内存直接响应，不再走跨海网络。
- 游戏动态请求（抽卡、编队、战斗结算、多人战等）不做任何缓存或修改，原样转发给上游代理。这部分延迟完全取决于上游代理节点质量，本工具无法改善。
- 内存缓存上限、资源预加载等性能选项可在界面“性能与系统资源选项”中调整。

【技术原理与免责声明】：
- 本工具为网络层本地静态资源缓存与透明代理，不修改任何游戏内数据、协议包或战斗参数。
- 游戏核心 API（抽卡、编队、结算、多人战等）原样转发至上游代理，保留 Cygames 原生响应头。
- 本工具不对账号安全作任何保证。使用第三方网络工具存在违反游戏服务条款的可能，是否使用请自行评估，风险自负。
